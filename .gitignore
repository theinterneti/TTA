# TTA .gitignore — curated for Python (uv), pytest, Neo4j, Redis, Docker, Nix, and common tooling

# -------------------------------------------------
# 1) Python bytecode and interpreter artifacts
# -------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# -------------------------------------------------
# 2) Build, packaging, and distribution artifacts
# -------------------------------------------------
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# -------------------------------------------------
# 3) Virtual environments (uv/venv/virtualenv/pyenv local)
# -------------------------------------------------
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
.python-version

# -------------------------------------------------
# 4) Testing & coverage outputs
# -------------------------------------------------
.pytest_cache/
.nox/
.hypothesis/
.benchmarks/
.coverage
.coverage.*
htmlcov/
coverage.xml
junit*.xml
.tox/
# Test result files that may contain sensitive data (JWT tokens, etc.)
*_test_results_*.json
*_test_results.json
*test_results.json
*_results.json
phase*_integration_test_results_*.json
integration_test_results.txt
test_run_output.txt
full_test_run_results.txt
verification-results.txt
# Test reports and artifacts
test-results/
test-results-*/
playwright-report/
# Coverage analysis files
coverage_*.json
narrative_arc_coverage_*.json
test_nao_coverage.json
# Mutation testing
mutation-report.html
*-mutation-report.html
cosmic-ray*.toml
session*.sqlite

# Batch processing and generation results
batch*_results.json
batch*_generation_results.json
batch*_manual_results.json

# Workflow and task queue files
task_queue.json
workflow_sync_report.json

# -------------------------------------------------
# 5) Tool caches (linters/type checkers/etc.)
# -------------------------------------------------
.mypy_cache/
.ruff_cache/
.pytype/
.pyre/
.pyright/
.cache/

# -------------------------------------------------
# 6) Logs and temporary files
# -------------------------------------------------
logs/
*.log
*.tmp
*.temp
.tmp/
.tmp/*
tmp/
temp/
# Temporary workspaces and artifacts
openhands_workspace/
chrome_profile_*/
notebooklm-mcp/
backups/
archive/
# Node modules (should not be in root)
node_modules/
# Build output
site/
# Batch processing results
batch*_results.json
batch*_generation_results.json
# Misc temporary files
emissions.csv
docker-warnings.json
task_queue.json
workflow_sync_report.json
cookies.txt
# Version number artifacts (likely typos/accidents)
=0.*.0

# Temporary workspaces and development artifacts
chrome_profile_notebooklm/
chrome_profile_*/
notebooklm-mcp/
backups/
tta-dev-codecov-setup/
secrets/

# Development output files
*_output.txt
query_notebook.py
simple_query.py
test_openhands_file_generation.py

# Storybook node_modules (should use workspace root)
src/player_experience/frontend/.storybook/node_modules/
src/player_experience/frontend/.storybook/package-lock.json

# Old/backup test files
tests/**/*.old
tests/**/*_v2.py

# -------------------------------------------------
# 7) Data & databases (local dev data only)
#    NOTE: This ignores generic data directories and common local DB files.
#    Adjust if you need to commit data samples.
# -------------------------------------------------
data/
*.db
*.sqlite3

# Neo4j (root-level and dev environment)
neo4j/data/
neo4j/logs/
neo4j/plugins/
# tta.dev Neo4j mounts
tta.dev/neo4j/data/
tta.dev/neo4j/logs/
tta.dev/neo4j/import/
tta.dev/neo4j/plugins/

# Redis (common local data directories / dumps)
redis/
redis-data/
dump.rdb

# -------------------------------------------------
# 8) Environment & secrets (do NOT commit secrets)
# -------------------------------------------------
# Environment files with actual secrets (NEVER commit these)
.env
.env.local
.env.development
.env.staging
.env.production
.env.test.local

# Legacy environment files (keep ignoring)
.env.*
*.env
*.env.*

# Exception: Allow .example files to be committed (they're templates)
!.env.example
!.env.local.example
!.env.production.example
!.env.staging.example
!src/player_experience/frontend/.env.example

# Secrets and certificates
.secrets/
*.pem
*.key
*.p12
*.pfx
*.crt
*.der
*.jks

# -------------------------------------------------
# 9) IDE/editor project files
# -------------------------------------------------
.idea/
.vscode/
*.iml
*.swp
*.swo
.history/
*.code-workspace

# -------------------------------------------------
# 10) OS-specific noise
# -------------------------------------------------
.DS_Store
._*
Thumbs.db
ehthumbs.db
Icon?
.AppleDouble
.LSOverride

# -------------------------------------------------
# 11) Docker & containers
# -------------------------------------------------
.docker/
# If you create local bind-mount directories for volumes, ignore them here:
# docker-data/

# -------------------------------------------------
# 12) Nix & direnv (local dev tooling)
# -------------------------------------------------
result
result-*
.direnv/
.envrc

# -------------------------------------------------
# 13) Project-specific caches and generated assets
# -------------------------------------------------
.model_cache/
huggingface-cache/

# Exclude specific directories that will be managed as submodules
# (These entries do not remove tracked files; they prevent new files from being added.)
/tta.dev/*
# Corrected prototype ignore (case-sensitive fix); keep the old line for safety below
/tta.prototype/*
/TTA.prototype/*config/neo4j-staging.conf
